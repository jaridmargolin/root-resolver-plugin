module.exports = {
  pkgB: require('pkg-b'),
  pkgC: require('pkg-c'),
  semverMatch: require('semver-match'),
  strMatch: require('str-match'),
  noMatch: require('no-match'),
  localModule: require('./local-module')
}
